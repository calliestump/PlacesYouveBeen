@using PlacesBeen.Models;

<h1>Places You've Been</h1>
@if (Model.Count == 0)
{
  <p>You apparently never left home.</p>
}
<ul>
  @foreach (Place place in Model)
  {
    <li><a href="/places/@place.Id">@place.CityName</a></li>
  }
</ul>
<a href="/places/new">Add a city.</a>

<form action="/places/delete" method = "post">
  <button type="submit" name="button">Clear All Cities</button>
</form>